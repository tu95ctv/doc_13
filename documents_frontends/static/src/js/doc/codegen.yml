overwrite: true
schema: schema.graphql
documents:
  - ./gql/query/*.gql
generates:
  src/codegen.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
