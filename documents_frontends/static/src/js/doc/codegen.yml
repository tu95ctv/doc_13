overwrite: true
schema: schema.json
documents:
  - ./gql/query/*.gql
generates:
  src/codegen.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
